engines:
  eslint:
    enabled: true
    channel: 'eslint-9'
    config:
      config: 'eslint.config.mjs'
    checks:
      complexity:
        enabled: false

ratings:
  paths:
    - '**.js'

checks:
  file-lines:
    config:
      threshold: 500
  method-lines:
    config:
      threshold: 50
  method-complexity:
    config:
      threshold: 11
